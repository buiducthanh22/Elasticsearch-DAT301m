from django.apps import AppConfig


class AppsearchConfig(AppConfig):
    """Application configuration for the `appsearch` application."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "appsearch"
